<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liucan.loda.ActorMapper">

    <resultMap id="actorResultMap" type="com.liucan.loda.Actor">
        <id column="actor_id" property="actorId"/>
        <result column="first_name" property="firstName"/>
        <result column="last_name" property="lastName"/>
        <result column="last_update" property="lastUpdate" javaType="java.time.LocalDateTime"/>
    </resultMap>

    <!-- 根据id获取用户信息 -->
    <select id="findUserById" parameterType="java.lang.Integer" resultMap="actorResultMap">
        select * from actor where actor_id = #{id}
    </select>

    <!-- 根据名称模糊查询用户列表 -->
    <select id="findUserByFirstname" parameterType="int" resultMap="actorResultMap">
        select * from actor where first_name like '%${value}%'
    </select>
</mapper>